---
import { SEO } from '../data/seo'
---

<section
  class="relative w-full py-20 md:py-28 overflow-hidden"
  style="background-image: url('/ambulance.png'); background-size: cover; background-position: center;"
>
  <!-- Overlay -->
  <div class="absolute inset-0 bg-black/40"></div>

  <!-- Content -->
  <div class="relative max-w-5xl mx-auto px-6 text-center text-white">
    <h1 class="text-3xl md:text-5xl font-bold mb-5 leading-tight">
      {SEO.headings.h1}
    </h1>

    <h2 class="text-lg md:text-2xl font-medium mb-6 opacity-95">
      {SEO.primaryIntent}
    </h2>

    <p class="text-base md:text-xl max-w-3xl mx-auto mb-10 leading-relaxed opacity-90">
      Asesoría independiente para personas y familias que buscan información clara sobre planes de
      rescate en ambulancia privada y asistencia médica prehospitalaria.
    </p>

    <h3 class="text-xl md:text-2xl font-semibold mb-8">
      Información y orientación para tu tranquilidad
    </h3>

    <!-- CTAs -->
    <div class="flex flex-col sm:flex-row gap-4 justify-center items-center">
      <a
        href={`tel:${SEO.identity.contact.phone}`}
        class="w-full sm:w-auto bg-white text-black font-semibold px-7 py-3 rounded-lg shadow-md hover:bg-gray-200 transition"
      >
        Llamar ahora
      </a>

      <a
        href={`https://wa.me/${SEO.identity.contact.whatsapp}?text=${encodeURIComponent(
          'Hola, quisiera recibir orientación sobre planes de asistencia privada.'
        )}`}
        class="w-full sm:w-auto bg-green-600 text-white font-semibold px-7 py-3 rounded-lg shadow-md hover:bg-green-700 transition"
      >
        Hablar por WhatsApp
      </a>
    </div>
  </div>
</section>
